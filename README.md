# ThesisCheck

Local multi-agent debate runner (Pro vs Contra + Judge) using an OpenAI-compatible local endpoint (e.g., LM Studio).

## Quick start
- copy `.env.example` -> `.env`
- run: `python -m thesis_check "Your thesis here"`